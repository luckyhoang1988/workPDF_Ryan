package stirling.software.proprietary.security.configuration.ee;

/** Legacy license tier labels kept for config/API compatibility in the full-MIT build. */
public enum PremiumLicenseTier {
    NORMAL,
    SERVER,
    ENTERPRISE
}
